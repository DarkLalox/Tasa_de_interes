import 'package:flutter/material.dart';

class Graphic_TIB extends StatelessWidget {
  const Graphic_TIB({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Tasa de interés interbancaria (TIB)'),
      ),
    );
  }
}
